
(tareas para Aurelio): Crear funcion en astrometria para hacer distribucion de healpix y otra en catalogo que haga el histograma healpix


1) Crea una opción, que si el plot2D, pueda pesar el color de cada punto por (usar seaborn.jointplot en scatter, variando el color en funcion de las siguientes variables): 
X - max/2 
Y - max/2
l
b
ra
dec

2) En catalogo crear una funcion que corrija flujos y magnitudes (y sus errores) por extinction

	rv_pars = {'DES':{'g':3.186, 'r':2.14, 'i':1.569, 'z':1.196, 'y':1.048},'SHARKS':{'Ks':0.308}}

	if mag <90 and mag>0:
		mag_corrected = mag - rv_pars * EBV
		magerr_corrected = magerr
	else:
		mag_corrected = mag
		magerr_corrected = magerr

	if flux>0:
		flux_corrected = flux * 10**(rv_pars/2.5)
		fluxerr_corrected = fluxerr * 10**(rv_pars/2.5)
	else:
		flux_corrected = flux
		fluxerr_corrected = fluxerr

3) Correr jupyter de ejemplo de eazy-py

4) cambiar self.area1 por self.area

5) 
	Crear un archivo en formato ascii con sharks y des con las columnas siguientes (las de flujo y error estan corregidas como te explico arriba!!): 

	COADD_OBJECT_ID FLUX_AUTO_G_CORRECTED FLUXERR_AUTO_G_CORRECTED FLUX_AUTO_R_CORRECTED FLUXERR_AUTO_R_CORRECTED FLUX_AUTO_I_CORRECTED FLUXERR_AUTO_I_CORRECTED FLUX_AUTO_Z_CORRECTED FLUXERR_AUTO_Z_CORRECTED FLUX_AUTO_Y_CORRECTED FLUXERR_AUTO_Y_CORRECTED KRONFLUX_CORRECTED KRONFLUXERR_CORRECTED

